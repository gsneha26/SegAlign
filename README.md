# WGA_GPU 
```
The system has been tested on all the AWS GPU instances with AMI Ubuntu Server 16.04 LTS (HVM), SSD Volume Type (ami-08cec7c429219e339 (64-bit x86))

```

```
Download and install software required for the tool
    $./install.sh

```
